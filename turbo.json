{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "plugin#build": {
      "outputs": ["dist/*.js"]
    },
    "plugin-ui#build": {
      "outputs": ["../src-plugin/dist/ui/**"]
    }
  }
}
